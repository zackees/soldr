name: Autonomous Release

on:
  push:
    branches:
      - main
    paths:
      - Cargo.toml
      - package.json
  workflow_dispatch:
    inputs:
      force_pypi_publish:
        description: Force the PyPI wheel build/publish path for the current Cargo version, even if PyPI already lists files for it.
        required: false
        type: boolean
        default: false
      npm_release_ref:
        description: Publish only the npm package from this git ref (for manual release recovery).
        type: string
        default: ""
permissions:
  contents: write
  attestations: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  # soldr#1322 — soldr's zccache/crgx fetch reads SOLDR_GITHUB_TOKEN
  # for authenticated GitHub Releases lookups (5,000/hr) instead of
  # unauthenticated (60/hr per runner IP). Follow-up to #1319 / #1323 /
  # #1326.
  SOLDR_GITHUB_TOKEN: ${{ github.token }}

concurrency:
  group: release-auto-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  prepare:
    name: Detect release version bump
    if: github.event_name != 'workflow_dispatch' || inputs.npm_release_ref == ''
    runs-on: ubuntu-24.04
    # Version/tag lookups only; observed max 0.2 min over 10 runs.
    timeout-minutes: 15
    outputs:
      should_release: ${{ steps.validate.outputs.should_release }}
      should_publish_github_release: ${{ steps.validate.outputs.should_publish_github_release }}
      should_publish_pypi: ${{ steps.validate.outputs.should_publish_pypi }}
      should_publish_npm: ${{ steps.validate.outputs.should_publish_npm }}
      tag_exists: ${{ steps.validate.outputs.tag_exists }}
      github_release_complete: ${{ steps.validate.outputs.github_release_complete }}
      github_release_immutable: ${{ steps.validate.outputs.github_release_immutable }}
      pypi_has_version: ${{ steps.validate.outputs.pypi_has_version }}
      pypi_file_count: ${{ steps.validate.outputs.pypi_file_count }}
      npm_has_version: ${{ steps.validate.outputs.npm_has_version }}
      version: ${{ steps.validate.outputs.version }}
      commit_sha: ${{ steps.validate.outputs.commit_sha }}
      build_matrix: ${{ steps.build_matrix.outputs.matrix }}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          submodules: recursive
          fetch-depth: 0

      # Contract-generated (soldr#2469 step 2.1): the build job's matrix
      # comes from ci/canonical-targets.json, so the matrix and the
      # completeness contract cannot drift apart separately again.
      - name: Generate release build matrix from the target contract
        id: build_matrix
        run: echo "matrix=$(python3 .github/scripts/release_completeness.py --build-matrix)" >> "$GITHUB_OUTPUT"

      - name: Compare candidate version against PyPI
        id: validate
        env:
          FORCE_PYPI_PUBLISH: ${{ github.event_name == 'workflow_dispatch' && inputs.force_pypi_publish || false }}
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          commit_sha='${{ github.sha }}'
          # `${VAR,,}` is bash 4.0. This job runs on ubuntu today, but the
          # rule is per-file for a reason: a step re-gated onto a darwin
          # lane would fail there with a parse error, not a clear message.
          force_pypi_publish="$(printf '%s' "${FORCE_PYPI_PUBLISH}" | tr '[:upper:]' '[:lower:]')"

          cargo_version="$(
            sed -n '/^\[workspace.package\]/,/^\[/{s/^version = "\([^"]*\)"/\1/p}' Cargo.toml | head -n1
          )"

          if [[ -z "$cargo_version" ]]; then
            echo "failed to derive workspace version from Cargo.toml" >&2
            exit 1
          fi

          version="v${cargo_version}"

          if [[ ! "$version" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.-]+)?$ ]]; then
            echo "derived version must look like vX.Y.Z" >&2
            exit 1
          fi

          npm_package_name="$(jq -r '.name // ""' package.json)"
          npm_package_version="$(jq -r '.version // ""' package.json)"
          if [[ -z "$npm_package_name" || -z "$npm_package_version" ]]; then
            echo "failed to derive npm package metadata from package.json" >&2
            exit 1
          fi
          if [[ "$npm_package_version" != "$cargo_version" ]]; then
            echo "package.json version ($npm_package_version) must match Cargo.toml ($cargo_version)" >&2
            exit 1
          fi

          tag_exists=false
          if git ls-remote --exit-code --tags origin "$version" >/dev/null 2>&1; then
            tag_exists=true
          fi

          github_release_complete=false
          github_release_immutable=false
          github_release_missing_assets="release-not-found"
          # Contract-generated (soldr#2469 step 2.2): one source of truth in
          # ci/canonical-targets.json via release_completeness.py, no inline
          # duplicate list. Captured first so a script failure fails the step.
          expected_asset_list="$(python3 .github/scripts/release_completeness.py --version "$version" --list-expected-github-assets)"
          expected_assets=()
          while IFS= read -r asset; do
            expected_assets+=("$asset")
          done <<< "$expected_asset_list"
          release_json="$(gh api "repos/${GITHUB_REPOSITORY}/releases/tags/${version}" 2>/dev/null || true)"
          if [[ -n "$release_json" ]]; then
            is_draft="$(jq -r '.draft' <<< "$release_json")"
            github_release_immutable="$(jq -r '.immutable // false' <<< "$release_json")"
            # No `mapfile`: this workflow also runs on macOS, whose bash is
            # 3.2. Keeping one style across the file means a step cannot be
            # re-gated onto a darwin lane and break there.
            actual_assets=()
            while IFS= read -r asset_name; do
              actual_assets+=("$asset_name")
            done < <(jq -r '.assets[].name' <<< "$release_json" | sort)
            missing=()
            for asset in "${expected_assets[@]}"; do
              if ! printf '%s\n' "${actual_assets[@]}" | grep -Fx "$asset" >/dev/null; then
                missing+=("$asset")
              fi
            done
            if [[ "$is_draft" == "false" && ${#missing[@]} -eq 0 ]]; then
              github_release_complete=true
              github_release_missing_assets=""
            else
              github_release_missing_assets="$(IFS=,; echo "${missing[*]}")"
              if [[ "$is_draft" != "false" ]]; then
                github_release_missing_assets="draft-release${github_release_missing_assets:+,$github_release_missing_assets}"
              fi
            fi
          fi

          pypi_json="$(curl -fsSL --max-time 30 --connect-timeout 10 https://pypi.org/pypi/soldr/json)"
          pypi_version="$(jq -r '.info.version // ""' <<< "$pypi_json")"

          if [[ -z "$pypi_version" || "$pypi_version" == "null" ]]; then
            echo "failed to fetch latest PyPI version for soldr" >&2
            exit 1
          fi

          pypi_file_count="$(
            jq --arg candidate "$cargo_version" -r '(.releases[$candidate] // []) | length' <<< "$pypi_json"
          )"
          pypi_has_version=false
          if (( pypi_file_count > 0 )); then
            pypi_has_version=true
          fi

          npm_package_uri="$(jq -rn --arg value "$npm_package_name" '$value | @uri')"
          npm_json="$(curl -fsSL --max-time 30 --connect-timeout 10 "https://registry.npmjs.org/${npm_package_uri}")"
          npm_has_version=false
          if jq -e --arg candidate "$cargo_version" '.versions[$candidate] != null' <<< "$npm_json" >/dev/null; then
            npm_has_version=true
          fi

          should_publish_github_release=false
          if [[ "$github_release_complete" != "true" && "$github_release_immutable" != "true" ]]; then
            should_publish_github_release=true
          fi

          should_publish_pypi=false
          if [[ "$pypi_has_version" != "true" || "$force_pypi_publish" == "true" ]]; then
            should_publish_pypi=true
          fi

          should_publish_npm=false
          if [[ "$npm_has_version" != "true" ]]; then
            should_publish_npm=true
          fi

          should_release=false
          if [[ "$should_publish_github_release" == "true" || "$should_publish_pypi" == "true" || "$should_publish_npm" == "true" ]]; then
            should_release=true
          fi

          {
            echo "### Release detection"
            echo ""
            echo "- candidate version: \`$version\`"
            echo "- current PyPI latest: \`v${pypi_version}\`"
            echo "- PyPI files for candidate: \`$pypi_file_count\`"
            echo "- npm package: \`$npm_package_name@$npm_package_version\`"
            echo "- npm has candidate: \`$npm_has_version\`"
            echo "- GitHub tag exists: \`$tag_exists\`"
            echo "- GitHub release complete: \`$github_release_complete\`"
            echo "- GitHub release immutable: \`$github_release_immutable\`"
            echo "- GitHub release missing assets: \`${github_release_missing_assets:-none}\`"
            echo "- force PyPI publish: \`$force_pypi_publish\`"
            echo "- publish GitHub release assets: \`$should_publish_github_release\`"
            echo "- publish PyPI wheels: \`$should_publish_pypi\`"
            echo "- publish npm package: \`$should_publish_npm\`"
          } >> "$GITHUB_STEP_SUMMARY"

          {
            echo "should_release=$should_release"
            echo "should_publish_github_release=$should_publish_github_release"
            echo "should_publish_pypi=$should_publish_pypi"
            echo "should_publish_npm=$should_publish_npm"
            echo "tag_exists=$tag_exists"
            echo "github_release_complete=$github_release_complete"
            echo "github_release_immutable=$github_release_immutable"
            echo "pypi_has_version=$pypi_has_version"
            echo "pypi_file_count=$pypi_file_count"
            echo "npm_has_version=$npm_has_version"
            echo "version=$version"
            echo "commit_sha=$commit_sha"
          } >> "$GITHUB_OUTPUT"

  build:
    name: ${{ matrix.name }}
    needs:
      - prepare
    if: needs.prepare.outputs.should_release == 'true'
    runs-on: ${{ matrix.runner }}
    # Windows x64's Linux cross-wheel takes roughly 37 minutes cold in the
    # Docker reproduction. Ninety minutes leaves room for runner variance.
    timeout-minutes: 90
    # Every release lane is required. Registry publishes are gated on a
    # verified GitHub Release asset set, so a missing target must stop the
    # release instead of silently shipping PyPI/npm only.
    continue-on-error: false
    strategy:
      fail-fast: false
      # Contract-generated (soldr#2469 step 2.1): per-target release build
      # metadata (runner, setup_target, binary — including the ARM64-musl
      # native-runner exception and the Linux-cross Windows lanes) lives in
      # ci/canonical-targets.json under `release.build`. The prepare job
      # emits the include list via release_completeness.py --build-matrix.
      matrix:
        include: ${{ fromJSON(needs.prepare.outputs.build_matrix) }}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          submodules: recursive
          ref: ${{ needs.prepare.outputs.commit_sha }}

      - name: Install native musl build tools
        if: contains(matrix.target, 'musl')
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends musl-tools mold cmake perl
          musl-gcc --version

      # ── Linux (soldr-driven) toolchain setup ──
      # soldr#1061 phase A: replaced dtolnay/rust-toolchain +
      # Swatinem/rust-cache with setup-soldr's integrated
      # `prebuild-deps: soldr-cook` flow so the release build benefits
      # from the same `target/<triple>/release/build/*/out/` cc-rs-
      # artifact restore that every-commit CI now uses.
      - name: Install pinned Soldr and prepare target environment
        id: setup_soldr
        continue-on-error: ${{ matrix.setup_target != '' }}
        uses: zackees/setup-soldr@40320d277ba4946e38d4b3c02e6c7a15a29c3f3f
        with:
          enable: true
          version: 0.8.44
          toolchain: 1.95.0
          cross-targets: ${{ matrix.setup_target }}
          linker: platform-default
          cache: false
          build-cache: false
          target-cache: false
          cargo-registry-cache: false
          soldr-mini-cache: false
          solo-toolchain-cache: false
          prebuild-deps: none
          zccache-seed-strict: false

      - name: Retry target preparation after setup transport failure
        if: steps.setup_soldr.outcome == 'failure' && matrix.setup_target != ''
        shell: bash
        run: python3 .github/scripts/prepare_release_target.py --target "${{ matrix.setup_target }}" --github-env "$GITHUB_ENV"

      - name: Restore the native macOS SDK root
        if: contains(matrix.target, 'apple-darwin')
        shell: bash
        run: echo "SDKROOT=$(xcrun --sdk macosx --show-sdk-path)" >> "$GITHUB_ENV"

      - name: Add cross-compile target
        shell: bash
        run: soldr rustup target add ${{ matrix.target }}

      # ── Build driver download (Linux only) ──
      # soldr#2294: the driver is built once in the bootstrap job and
      # shared. Download it to the exact path the per-lane `cargo build`
      # used to produce, so every later `driver="target/release/soldr"`
      # reference (archive packaging, support-binary extraction, smoke
      # steps) is unchanged.
      - name: Materialize installed Soldr as release build driver
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path target/release | Out-Null
          $source = (Get-Command soldr -ErrorAction Stop).Source
          if ($env:RUNNER_OS -eq 'Windows') {
            Copy-Item -LiteralPath $source -Destination target/release/soldr.exe -Force
          } else {
            Copy-Item -LiteralPath $source -Destination target/release/soldr -Force
          }

      - name: Restore executable bit on bootstrap driver
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS" in
            Windows) driver="target/release/soldr.exe" ;;
            *) driver="target/release/soldr"; chmod +x "$driver" ;;
          esac
          "$driver" --version

      # ── Build release binary ──
      - name: Build release binary (soldr-driven)
        if: matrix.target != 'aarch64-unknown-linux-musl' && !contains(matrix.target, 'apple-darwin')
        shell: bash
        env:
          SOLDR_RELEASE_CI: "1"
          CARGO_PROFILE_RELEASE_DEBUG: "0"
          CARGO_PROFILE_RELEASE_LTO: ${{ contains(matrix.target, 'pc-windows-msvc') && 'false' || 'thin' }}
          CARGO_PROFILE_RELEASE_CODEGEN_UNITS: ${{ contains(matrix.target, 'pc-windows-msvc') && '16' || '1' }}
          # soldr#2453: bound Cargo's producer queue and zccache's admission
          # gate so the swap headroom above is a safety valve, not the primary
          # defense. These lanes previously ran unbounded on a 4-vCPU runner.
          CARGO_BUILD_JOBS: "2"
          SOLDR_JOBS: "2"
        run: |
          set -euo pipefail
          git restore -- Cargo.toml Cargo.lock crates/soldr-cli/Cargo.toml || true
          target='${{ matrix.target }}'
          case "$RUNNER_OS" in
            Windows) driver="target/release/soldr.exe" ;;
            *) driver="target/release/soldr" ;;
          esac
          "$driver" cargo clean -p soldr-cli --target "$target" --release 2>/dev/null || true
          # Avoid the resident embedded daemon that caused the old
          # Linux-hosted cross-build memory collision.
          soldr_build_args=(--no-cache)
          "$driver" \
            "${soldr_build_args[@]}" \
            build --release --target "$target" \
            --package soldr-cli \
            --bin soldr

          if [[ "$target" == *-unknown-linux-gnu ]]; then
            "$driver" prepare --target "$target" --github-env "$GITHUB_ENV"
          fi
          echo "=== post-build diagnostic: target/$target/release/ ==="
          ls -la "target/$target/release/" 2>&1 | head -20 || true
          echo "==================================================="

      - name: Build ARM64 musl release binary natively
        if: matrix.target == 'aarch64-unknown-linux-musl'
        shell: bash
        env:
          CC_aarch64_unknown_linux_musl: musl-gcc
          CXX_aarch64_unknown_linux_musl: musl-gcc
          AR_aarch64_unknown_linux_musl: ar
          RANLIB_aarch64_unknown_linux_musl: ranlib
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER: musl-gcc
        run: |
          set -euo pipefail
          # The catalogue ARM64-musl compiler shipped for soldr 0.8.44 is an
          # i386-hosted cross compiler. It cannot execute on the native ARM64
          # runner, so bypass target preparation while still routing Cargo
          # through the pinned Soldr and rustup toolchain.
          python3 .github/scripts/native_release_build.py binary \
            --driver target/release/soldr --target aarch64-unknown-linux-musl

      - name: Build native macOS release binary through pinned Soldr
        if: contains(matrix.target, 'apple-darwin')
        shell: bash
        run: |
          set -euo pipefail
          # Native macOS already has the correct Xcode SDK and clang. Soldr
          # 0.8.44's blessed build path adds its prepared SDK directory as a
          # bare linker input, so invoke Cargo through pinned Soldr/rustup
          # without repeating target preparation.
          python3 .github/scripts/native_release_build.py binary \
            --driver target/release/soldr --target "${{ matrix.target }}"

      # soldr#1060 acceptance: the musl artifacts are the "runs anywhere"
      # build precisely because they carry no dynamic dependencies. A musl
      # target that silently picked up a dynamic link still builds and still
      # passes tests on this modern image -- it only fails on the old distro
      # the artifact exists to serve. Verify before staging, so a bad binary
      # is never published.
      - name: Verify musl binary is statically linked (soldr#1060)
        if: contains(matrix.target, 'musl')
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/verify_static_link.py \
            "target/${{ matrix.target }}/release/${{ matrix.binary }}"

      # soldr#1060 names glibc 2.17 as the baseline for the -gnu artifacts,
      # and warns "do not build on modern Ubuntu and call it portable". That
      # is what happens today: these are built natively here, so the
      # published v0.8.29 binaries require GLIBC_2.39 (measured, x86_64 and
      # aarch64) and cannot start on RHEL 9, Debian 12 or Ubuntu 22.04 --
      # the distros the fallback exists for.
      #
      # The ceiling below is therefore the CURRENT measured floor, not the
      # target: a ratchet, in the same spirit as loc_ratchet. It cannot break
      # today's release, and it cannot silently get worse. The real fix is to
      # link against the baseline (zigbuild `<triple>.2.17` or an old-glibc
      # container), after which this drops to 2.17 and becomes a real gate.
      #
      # If a runner image bump raises the floor, this step failing IS the
      # intended signal -- raise the ceiling deliberately, with a note, rather
      # than by reflex.
      - name: Check glibc baseline of gnu binary (soldr#1060 ratchet)
        if: contains(matrix.target, 'unknown-linux-gnu')
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/verify_glibc_baseline.py \
            --max-glibc 2.39 \
            "target/${{ matrix.target }}/release/${{ matrix.binary }}"

      - name: Stage soldr release binaries
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist/package
          suffix=""
          case "${{ matrix.target }}" in *-pc-windows-msvc) suffix=".exe" ;; esac

          src="target/${{ matrix.target }}/release/soldr${suffix}"
          if [ ! -f "$src" ]; then
            echo "expected soldr${suffix} in target dir; observed release dir:" >&2
            ls -la "target/${{ matrix.target }}/release" >&2 || true
            exit 1
          fi
          cp "$src" "dist/package/soldr${suffix}"
          for stem in soldr-daemon; do
            ln "dist/package/soldr${suffix}" "dist/package/${stem}${suffix}" 2>/dev/null || \
              cp "dist/package/soldr${suffix}" "dist/package/${stem}${suffix}"
          done
          chmod +x dist/package/* 2>/dev/null || true

          # soldr#1302: toolchain, clang, and zccache wrapper shims are
          # hardlinks/copies of `soldr` materialized at install time.
          # There are no shim sidecar binaries to stage, so the #1032
          # missing-sidecar archive class is structurally impossible.
          case "${{ matrix.target }}" in
            *-pc-windows-msvc)
              # Windows MSVC always emits a `.pdb` next to the binary
              # under the default release profile. Required (#782).
              pdb_src=""
              for candidate in \
                "target/${{ matrix.target }}/release/soldr.pdb" \
                "target/${{ matrix.target }}/release/soldr_cli.pdb"; do
                if [ -f "$candidate" ]; then
                  pdb_src="$candidate"
                  break
                fi
              done
              if [ -z "$pdb_src" ]; then
                echo "expected a soldr PDB sidecar next to ${{ matrix.binary }}; observed release dir:" >&2
                ls -la "target/${{ matrix.target }}/release" >&2 || true
                exit 1
              fi
              cp "$pdb_src" "dist/package/$(basename "$pdb_src")"
              ;;
            *-unknown-linux-*)
              # Linux split DWARF: only present when the release
              # profile sets `split-debuginfo = "packed"`. The default
              # Rust release profile DOES NOT, so a missing `.dwp` is
              # non-fatal — we stage it opportunistically. Issue #786:
              # generalize debug-sidecar packaging beyond Windows. The
              # manifest writer below records whatever ends up in
              # `dist/package/` under `soldr.debug_info`.
              for candidate in \
                "target/${{ matrix.target }}/release/soldr.dwp" \
                "target/${{ matrix.target }}/release/soldr_cli.dwp"; do
                if [ -f "$candidate" ]; then
                  cp "$candidate" "dist/package/$(basename "$candidate")"
                  echo "staged Linux split-DWARF sidecar: $(basename "$candidate")"
                  break
                fi
              done
              ;;
            *-apple-darwin)
              # macOS dSYM bundle: only present when the release
              # profile sets `split-debuginfo = "packed"`. The bundle
              # is a directory tree, NOT a single file — `cp -R`
              # preserves it. Missing bundle is non-fatal (#786).
              for candidate in \
                "target/${{ matrix.target }}/release/soldr.dSYM" \
                "target/${{ matrix.target }}/release/soldr_cli.dSYM"; do
                if [ -d "$candidate" ]; then
                  cp -R "$candidate" "dist/package/$(basename "$candidate")"
                  echo "staged macOS dSYM sidecar bundle: $(basename "$candidate")"
                  break
                fi
              done
              ;;
          esac
          ls -la dist/package/

      - name: Fetch support binaries from soldr-toolchain
        shell: bash
        env:
          TARGET_TRIPLE: ${{ matrix.target }}
          SUPPORT_ISSUE_URL: https://github.com/zackees/soldr-toolchain/issues/47
          SOLD_DRIVER_PATH: target/release/soldr
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          import json
          import os
          import shutil
          import subprocess
          import sys
          import tarfile
          import tempfile
          import urllib.request
          from urllib.parse import urljoin
          import zipfile
          from pathlib import Path

          ORIGIN = os.environ.get("SOLDR_TOOLCHAIN_ORIGIN", "https://zackees.github.io/soldr-toolchain").rstrip("/")
          TARGET = os.environ["TARGET_TRIPLE"]
          ISSUE = os.environ["SUPPORT_ISSUE_URL"]
          OUT = Path("dist/package")
          OUT.mkdir(parents=True, exist_ok=True)

          def read_constant(path: str, name: str) -> str:
              import re
              text = Path(path).read_text(encoding="utf-8")
              m = re.search(rf'{name}\s*:\s*&str\s*=\s*"([^"]+)"', text)
              if not m:
                  raise SystemExit(f"could not read {name} from {path}")
              return m.group(1)

          def platform_for_target(target: str) -> dict[str, str]:
              table = {
                  "x86_64-unknown-linux-gnu": {"os": "linux", "arch": "x86_64", "libc": "glibc"},
                  "aarch64-unknown-linux-gnu": {"os": "linux", "arch": "aarch64", "libc": "glibc"},
                  "x86_64-unknown-linux-musl": {"os": "linux", "arch": "x86_64", "libc": "musl"},
                  "aarch64-unknown-linux-musl": {"os": "linux", "arch": "aarch64", "libc": "musl"},
                  "x86_64-apple-darwin": {"os": "darwin", "arch": "x86_64"},
                  "aarch64-apple-darwin": {"os": "darwin", "arch": "aarch64"},
                  "x86_64-pc-windows-msvc": {"os": "windows", "arch": "x86_64", "abi": "msvc"},
                  "aarch64-pc-windows-msvc": {"os": "windows", "arch": "aarch64", "abi": "msvc"},
              }
              try:
                  return table[target]
              except KeyError:
                  raise SystemExit(f"no soldr-toolchain platform mapping for {target}")

          def read_url(url: str, timeout: int) -> bytes:
              with urllib.request.urlopen(url, timeout=timeout) as resp:
                  return resp.read()

          def load_json(url: str):
              return json.loads(read_url(url, timeout=60).decode("utf-8"))

          def download(url: str, dest: Path) -> None:
              dest.write_bytes(read_url(url, timeout=600))

          def sha256(path: Path) -> str:
              h = hashlib.sha256()
              with path.open("rb") as fh:
                  for chunk in iter(lambda: fh.read(1024 * 1024), b""):
                      h.update(chunk)
              return h.hexdigest()

          def sha256_bytes(data: bytes) -> str:
              return hashlib.sha256(data).hexdigest()

          INDEX_URL = f"{ORIGIN}/manifest.json"
          INDEX = load_json(INDEX_URL)

          def load_tool_catalog(tool: str):
              entry = (INDEX.get("tools") or {}).get(tool)
              descriptor = (entry or {}).get("descriptor") or {}
              descriptor_url = descriptor.get("url")
              if not descriptor_url:
                  raise SystemExit(f"{INDEX_URL} has no descriptor URL for {tool}; support coverage is tracked in {ISSUE}")
              catalog_url = urljoin(f"{ORIGIN}/", descriptor_url)
              raw = read_url(catalog_url, timeout=60)
              expected_sha = str(descriptor.get("sha256") or "").lower()
              if expected_sha and sha256_bytes(raw) != expected_sha:
                  raise SystemExit(f"{catalog_url} sha256 mismatch against {INDEX_URL} descriptor")
              return catalog_url, json.loads(raw.decode("utf-8"))

          def extract_archive(archive: Path, extract_dir: Path) -> None:
              name = archive.name
              if name.endswith(".tar.zst"):
                  # soldr#2453: native builds put the binary under the
                  # target-triple directory; Linux cross-builds use the
                  # bootstrap driver at target/release/soldr.
                  # Windows binaries carry a .exe suffix.
                  suffix = ".exe" if os.name == "nt" else ""
                  driver_candidates = [
                      Path(os.environ["SOLD_DRIVER_PATH"]),
                      Path(os.environ["SOLD_DRIVER_PATH"] + suffix),
                      Path(f"target/{os.environ['TARGET_TRIPLE']}/release/soldr{suffix}"),
                      Path(f"target/release/soldr{suffix}"),
                  ]
                  driver = None
                  for c in driver_candidates:
                      if c.is_file():
                          driver = c
                          break
                  if driver is None:
                      raise SystemExit(f"no soldr driver found; tried {driver_candidates}")
                  if not driver.is_file():
                      raise SystemExit(f"checked-out soldr driver missing at {driver}")
                  subprocess.run(
                      [
                          str(driver),
                          "archive",
                          "--input",
                          str(archive),
                          "--extract-dir",
                          str(extract_dir),
                      ],
                      check=True,
                  )
              elif name.endswith(".zip"):
                  with zipfile.ZipFile(archive) as zf:
                      zf.extractall(extract_dir)
              elif name.endswith(".tar.gz") or name.endswith(".tgz"):
                  with tarfile.open(archive, "r:gz") as tf:
                      tf.extractall(extract_dir)
              else:
                  raise SystemExit(f"unsupported support archive format: {archive}")

          def fetch_tool(tool: str, version: str) -> None:
              release_version = version if version.startswith("v") else f"v{version}"
              catalog_url, catalog = load_tool_catalog(tool)
              release = next((r for r in catalog.get("releases", []) if r.get("version") == release_version), None)
              if release is None:
                  raise SystemExit(
                      f"{catalog_url} has no release {release_version}; support coverage is tracked in {ISSUE}"
                  )
              platform = platform_for_target(TARGET)
              platform_entry = next((p for p in release.get("platforms", []) if p.get("platform") == platform), None)
              if platform_entry is None:
                  raise SystemExit(
                      f"{catalog_url} release {release_version} has no asset for {TARGET} ({platform}); "
                      f"support coverage is tracked in {ISSUE}"
                  )
              asset = platform_entry["asset"]
              urls = asset.get("urls") or []
              expected_sha = str(asset.get("sha256", "")).lower()
              if not urls or len(expected_sha) != 64:
                  raise SystemExit(f"{catalog_url} release {release_version} asset for {TARGET} lacks url/sha256")
              suffix = ".exe" if TARGET.endswith("pc-windows-msvc") else ""
              with tempfile.TemporaryDirectory(prefix=f"{tool}-support-") as td:
                  tmp = Path(td)
                  archive = tmp / asset["filename"]
                  print(f"fetching {tool} {release_version} for {TARGET}: {urls[0]}")
                  download(urls[0], archive)
                  actual_sha = sha256(archive)
                  if actual_sha != expected_sha:
                      raise SystemExit(f"{tool} sha256 mismatch: expected {expected_sha}, got {actual_sha}")
                  extract_dir = tmp / "extract"
                  extract_dir.mkdir()
                  extract_archive(archive, extract_dir)
                  binary_name = f"{tool}{suffix}"
                  matches = [p for p in extract_dir.rglob(binary_name) if p.is_file()]
                  if not matches:
                      raise SystemExit(f"{tool} support archive did not contain {binary_name}")
                  matches.sort(key=lambda p: (0 if p.parent.name == "bin" else 1, len(p.parts), str(p)))
                  dest = OUT / binary_name
                  shutil.copy2(matches[0], dest)
                  dest.chmod(dest.stat().st_mode | 0o755)
                  env_name = "CRGX_SOURCE_COMMIT" if tool == "crgx" else "CARGO_CHEF_SOURCE_COMMIT"
                  # A real newline, not `\\n`. Inside this block scalar Python
                  # reads `\\n` as backslash-plus-n, so both assignments landed
                  # on ONE line of $GITHUB_ENV: crgx's value swallowed the
                  # cargo-chef assignment and CARGO_CHEF_SOURCE_COMMIT was
                  # never set at all. Published v0.8.29 shows the result --
                  # crgx.source_commit is
                  # "soldr-toolchain:v0.1.0\nCARGO_CHEF_SOURCE_COMMIT=soldr-toolchain:v0.1.73"
                  # and cargo_chef.source_commit is "unknown".
                  with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as fh:
                      fh.write(f"{env_name}=soldr-toolchain:{release_version}\n")
                  print(f"staged {dest} from {matches[0]}")

          versions = {
              "crgx": read_constant("crates/soldr-fetch/src/fetch/mod.rs", "MANAGED_CRGX_VERSION"),
              "cargo-chef": read_constant("crates/soldr-fetch/src/fetch/known_tools.rs", "CARGO_CHEF_PINNED_VERSION"),
          }
          fetch_tool("crgx", versions["crgx"])
          fetch_tool("cargo-chef", versions["cargo-chef"])
          PY
          ls -la dist/package/

      # soldr#1060: the musl bundle is the "runs anywhere" artifact, but only
      # `soldr` was ever verified -- and the bundle ships four binaries. crgx
      # and cargo-chef are fetched here, after the earlier check has already
      # run, so nothing looked at them.
      #
      # They are fine today (checked against published v0.8.29: both are
      # static-PIE and run on Debian 12 and Alpine alike). The point is that
      # if one ever came back dynamically linked, the artifact would keep its
      # "static" promise for soldr and quietly break for the tools soldr
      # shells out to.
      #
      # This could not be added before the static-PIE fix in this same change:
      # the old "no dynamic section" test called both of them dynamic, so
      # extending the check here would have failed the release on correct
      # binaries.
      # soldr#1060, Windows side -- the last release surface with no
      # portability guard. A PE that imports a DLL the machine lacks does not
      # degrade gracefully; it fails to start.
      #
      # Measured on published v0.8.29: soldr.exe and soldr-daemon.exe import
      # 22 DLLs, of which exactly one is not part of Windows --
      # VCRUNTIME140.dll, from the Visual C++ Redistributable. The
      # api-ms-win-crt-* entries beside it are UCRT forwarders that DO ship
      # with Windows 10+, so they are not a dependency in the same sense.
      #
      # Nothing sets `-C target-feature=+crt-static`, so that is Rust's MSVC
      # default rather than a decision; a static CRT would remove it outright.
      # This step does not make that build change -- it pins the current
      # import set so a NEW non-system dependency cannot appear unnoticed, and
      # names the redistributable one on every pass so the cost stays visible.
      - name: Ratchet the Windows DLL imports (soldr#1060)
        if: contains(matrix.target, 'pc-windows-msvc')
        shell: bash
        run: |
          set -euo pipefail
          # bash 3.2 -- what macOS ships, frozen there by GPLv3 -- has no
          # `mapfile`. A read loop is the portable equivalent and behaves
          # identically on every runner.
          bundled=()
          while IFS= read -r bundled_path; do
            bundled+=("$bundled_path")
          done < <(find dist/package -maxdepth 1 -type f \
            \( -name 'soldr.exe' -o -name 'soldr-daemon.exe' \
               -o -name 'crgx.exe' -o -name 'cargo-chef.exe' \))
          if [ "${#bundled[@]}" -eq 0 ]; then
            echo "no bundled binaries found in dist/package to verify" >&2
            ls -la dist/package >&2
            exit 1
          fi
          python3 .github/scripts/verify_windows_imports.py "${bundled[@]}"

      # soldr#1060, macOS side. A Mach-O records the oldest OS it will start
      # on, and nothing watched that value: it is whatever the toolchain and
      # the pinned Apple SDK produce, since no lane sets
      # MACOSX_DEPLOYMENT_TARGET. Drift upward has the same shape as the
      # GLIBC_2.39 problem -- builds fine, tests fine, refuses to launch on
      # older Macs, and you find out from a user.
      #
      # Measured on published v0.8.29: aarch64 was 11.0 throughout (correct and
      # unavoidable for Apple Silicon), and on x86_64 soldr was *also* 11.0
      # while crgx beside it was 10.12 -- so the bundle was not internally
      # consistent and x86_64 asked for more than Rust's 10.12 default for that
      # target. soldr#2146 fixed the cause (five hardcoded 11.0 literals in the
      # Darwin arm of blessed build, applied to both arches) and this ceiling is
      # now per-arch to match, so an x86_64 binary that drifts back up to 11.0
      # fails here instead of passing against a ceiling set too loose to bind.
      #
      # The ceilings live in verify_macos_min_version.py beside the parsing,
      # not inline here -- they must stay in step with
      # crates/soldr-cli/src/blessed_build/darwin_arch.rs.
      - name: Ratchet the macOS minimum version (soldr#1060)
        if: contains(matrix.target, 'apple-darwin')
        shell: bash
        run: |
          set -euo pipefail
          # bash 3.2 -- what macOS ships, frozen there by GPLv3 -- has no
          # `mapfile`. A read loop is the portable equivalent and behaves
          # identically on every runner.
          bundled=()
          while IFS= read -r bundled_path; do
            bundled+=("$bundled_path")
          done < <(find dist/package -maxdepth 1 -type f \
            \( -name soldr -o -name soldr-daemon -o -name crgx -o -name cargo-chef \))
          if [ "${#bundled[@]}" -eq 0 ]; then
            echo "no bundled binaries found in dist/package to verify" >&2
            ls -la dist/package >&2
            exit 1
          fi
          python3 .github/scripts/verify_macos_min_version.py \
            --target "${{ matrix.target }}" "${bundled[@]}"

      - name: Verify every bundled musl binary is static (soldr#1060)
        if: contains(matrix.target, 'musl')
        shell: bash
        run: |
          set -euo pipefail
          # bash 3.2 -- what macOS ships, frozen there by GPLv3 -- has no
          # `mapfile`. A read loop is the portable equivalent and behaves
          # identically on every runner.
          bundled=()
          while IFS= read -r bundled_path; do
            bundled+=("$bundled_path")
          done < <(find dist/package -maxdepth 1 -type f \
            \( -name soldr -o -name soldr-daemon -o -name crgx -o -name cargo-chef \))
          if [ "${#bundled[@]}" -eq 0 ]; then
            echo "no bundled binaries found in dist/package to verify" >&2
            ls -la dist/package >&2
            exit 1
          fi
          python3 .github/scripts/verify_static_link.py "${bundled[@]}"

      # The gnu counterpart of the musl check above, and for the same reason
      # #2084 extended that one: the bundle ships FOUR binaries, and the
      # earlier glibc ratchet reads only `soldr` out of target/, before
      # staging. crgx and cargo-chef are never looked at.
      #
      # They are the ones that most need watching. Both are fetched prebuilt
      # from soldr-toolchain rather than built here, so their floor can move
      # without a single line of soldr changing -- exactly the external drift
      # a ratchet exists to catch. A user whose glibc is too old for a bundled
      # tool gets a soldr that starts and then fails the moment it shells out.
      #
      # Measured on published v0.8.29, all eight binaries across both gnu
      # bundles sit at or below this ceiling (aarch64 cargo-chef is 2.18), so
      # this reports today rather than blocking.
      - name: Check glibc baseline of every bundled gnu binary (soldr#1060)
        if: contains(matrix.target, 'unknown-linux-gnu')
        shell: bash
        run: |
          set -euo pipefail
          # bash 3.2 -- what macOS ships, frozen there by GPLv3 -- has no
          # `mapfile`. A read loop is the portable equivalent and behaves
          # identically on every runner.
          bundled=()
          while IFS= read -r bundled_path; do
            bundled+=("$bundled_path")
          done < <(find dist/package -maxdepth 1 -type f \
            \( -name soldr -o -name soldr-daemon -o -name crgx -o -name cargo-chef \))
          if [ "${#bundled[@]}" -eq 0 ]; then
            echo "no bundled binaries found in dist/package to verify" >&2
            ls -la dist/package >&2
            exit 1
          fi
          python3 .github/scripts/verify_glibc_baseline.py \
            --max-glibc 2.39 "${bundled[@]}"

      - name: Write manifest.json
        # Drop a small JSON descriptor into the archive root so downstream
        # consumers (setup-soldr, npm install wrapper, ad-hoc tooling)
        # have one stable source of truth for what's inside: soldr +
        # zccache versions, target triples, per-binary sha256s, archive
        # format. Schema is versioned so we can extend later without
        # breaking parsers.
        shell: bash
        run: |
          set -euo pipefail
          version="${{ needs.prepare.outputs.version }}"
          commit_sha="${{ needs.prepare.outputs.commit_sha }}"
          soldr_target="${{ matrix.target }}"
          zccache_version=$(sed -n 's/^version = "\(.*\)"/\1/p' \
            _vender/zccache/Cargo.toml | head -n1)
          crgx_version=$(sed -n 's/.*MANAGED_CRGX_VERSION: &str = "\(.*\)";/\1/p' \
            crates/soldr-fetch/src/fetch/mod.rs | head -n1)
          cargo_chef_version=$(sed -n 's/.*CARGO_CHEF_PINNED_VERSION: &str = "\(.*\)";/\1/p' \
            crates/soldr-fetch/src/fetch/known_tools.rs | head -n1)
          if [ -z "$crgx_version" ]; then
            echo "could not read MANAGED_CRGX_VERSION from crates/soldr-fetch/src/fetch/mod.rs" >&2
            exit 1
          fi
          if [ -z "$cargo_chef_version" ]; then
            echo "could not read CARGO_CHEF_PINNED_VERSION from crates/soldr-fetch/src/fetch/known_tools.rs" >&2
            exit 1
          fi
          crgx_commit="${CRGX_SOURCE_COMMIT:-unknown}"
          cargo_chef_commit="${CARGO_CHEF_SOURCE_COMMIT:-unknown}"
          zccache_target="$soldr_target"
          suffix=""
          case "$soldr_target" in *-pc-windows-msvc) suffix=".exe" ;; esac

          sha256_of() {
            # Cross-platform sha256: macOS lacks `sha256sum` by default,
            # so fall back to `shasum -a 256` (or fail loudly).
            if command -v sha256sum >/dev/null 2>&1; then
              sha256sum "$1" | awk '{print $1}'
            else
              shasum -a 256 "$1" | awk '{print $1}'
            fi
          }

          soldr_sha=$(sha256_of "dist/package/soldr${suffix}")
          # Build `soldr.debug_info` from every sidecar staged above
          # (issue #786). Windows .pdb is REQUIRED — its absence fails
          # the build. Linux .dwp / macOS .dSYM are OPTIONAL — the
          # default Rust release profile doesn't emit them, so a
          # missing sidecar is silent and the array stays empty. Format
          # tags MUST match the constants in
          # `crates/soldr-cli/src/release_sidecar.rs::DebugSidecarFormat::as_manifest_str`.
          soldr_debug_info_entries=()
          add_sidecar_entry() {
            local path="$1" format="$2"
            local name sha
            name=$(basename "$path")
            sha=$(sha256_of "$path")
            soldr_debug_info_entries+=("{ \"name\": \"${name}\", \"sha256\": \"${sha}\", \"format\": \"${format}\" }")
          }
          if [ -n "$suffix" ]; then
            soldr_pdb=$(find dist/package -maxdepth 1 -type f \( -name 'soldr.pdb' -o -name 'soldr_cli.pdb' \) -print -quit)
            if [ -z "$soldr_pdb" ]; then
              echo "missing soldr PDB sidecar in dist/package for Windows release" >&2
              ls -la dist/package >&2
              exit 1
            fi
            add_sidecar_entry "$soldr_pdb" "pdb"
          fi
          soldr_dwp=$(find dist/package -maxdepth 1 -type f \( -name 'soldr.dwp' -o -name 'soldr_cli.dwp' \) -print -quit)
          if [ -n "$soldr_dwp" ]; then
            add_sidecar_entry "$soldr_dwp" "dwp"
          fi
          # macOS dSYM is a directory; sha256 it as a tarball of its
          # contents so consumers can verify the whole bundle in one
          # check. The bundle layout itself is preserved on disk.
          # Pipe through whichever sha256 binary is available — the
          # `sha256_of` helper above takes a path arg, not stdin.
          soldr_dsym=$(find dist/package -maxdepth 1 -type d \( -name 'soldr.dSYM' -o -name 'soldr_cli.dSYM' \) -print -quit)
          if [ -n "$soldr_dsym" ]; then
            dsym_name=$(basename "$soldr_dsym")
            if command -v sha256sum >/dev/null 2>&1; then
              dsym_sha=$(tar -cf - -C "$(dirname "$soldr_dsym")" "$dsym_name" | sha256sum | awk '{print $1}')
            else
              dsym_sha=$(tar -cf - -C "$(dirname "$soldr_dsym")" "$dsym_name" | shasum -a 256 | awk '{print $1}')
            fi
            soldr_debug_info_entries+=("{ \"name\": \"${dsym_name}\", \"sha256\": \"${dsym_sha}\", \"format\": \"dsym\" }")
          fi
          if [ ${#soldr_debug_info_entries[@]} -eq 0 ]; then
            soldr_debug_info_json="[]"
          else
            soldr_debug_info_json=$(printf "[%s]" "$(IFS=,; echo "${soldr_debug_info_entries[*]}")")
          fi
          soldr_daemon_sha=$(sha256_of "dist/package/soldr-daemon${suffix}")
          crgx_sha=$(sha256_of "dist/package/crgx${suffix}")
          cargo_chef_sha=$(sha256_of "dist/package/cargo-chef${suffix}")
          built_at=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          cat > dist/package/manifest.json <<EOF
          {
            "schema_version": 3,
            "soldr": {
              "version": "${version}",
              "target": "${soldr_target}",
              "binary": "soldr${suffix}",
              "sha256": "${soldr_sha}",
              "sidecars": [
                { "name": "soldr-daemon${suffix}", "sha256": "${soldr_daemon_sha}" }
              ],
              "debug_info": ${soldr_debug_info_json},
              "commit_sha": "${commit_sha}"
            },
            "zccache": {
              "version": "${zccache_version}",
              "target": "${zccache_target}",
              "embedded": true
            },
            "crgx": {
              "version": "${crgx_version}",
              "target": "${soldr_target}",
              "binary": "crgx${suffix}",
              "sha256": "${crgx_sha}",
              "source_commit": "${crgx_commit}"
            },
            "cargo_chef": {
              "version": "${cargo_chef_version}",
              "target": "${soldr_target}",
              "binary": "cargo-chef${suffix}",
              "sha256": "${cargo_chef_sha}",
              "source_commit": "${cargo_chef_commit}"
            },
            "archive": {
              "format": "tar.zst",
              "compression_level": 19
            },
            "built_at": "${built_at}"
          }
          EOF

          echo "--- manifest.json ---"
          cat dist/package/manifest.json

      # manifest.json is the release describing itself, and setup-soldr, the
      # npm wrapper and ad-hoc tooling all read it. Nothing checked it, and it
      # was wrong: published v0.8.29 has crgx.source_commit holding
      # "soldr-toolchain:v0.1.0\nCARGO_CHEF_SOURCE_COMMIT=soldr-toolchain:v0.1.73"
      # with cargo_chef.source_commit reading "unknown", because the step
      # writing $GITHUB_ENV terminated lines with a literal backslash-n (fixed
      # in this change). The digests were all correct -- this was provenance,
      # not integrity -- but provenance that silently says "unknown" is worse
      # than absent, because it looks answered.
      #
      # --package-dir re-derives every declared sha256 from the staged bytes,
      # so the manifest cannot describe an archive it does not match.
      - name: Verify manifest.json describes what is staged (soldr#1060)
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/verify_release_manifest.py \
            dist/package/manifest.json \
            --package-dir dist/package

      - name: Package combined archive (tar.zst level 19)
        # Use soldr's in-process Rust zstd encoder rather than depending
        # on a runner-local `zstd` executable. The command uses level 19
        # and all available cores.
        shell: bash
        run: |
          set -euo pipefail
          version="${{ needs.prepare.outputs.version }}"
          name="soldr-${version}-${{ matrix.target }}"
          # soldr#2453: native builds use the just-built binary with the
          # platform-appropriate suffix; Linux cross-builds use the
          # bootstrap driver.
          case "$RUNNER_OS" in
            Windows) driver_suffix=".exe" ;;
            *) driver_suffix="" ;;
          esac
          driver="target/release/soldr${driver_suffix}"
          "$driver" archive --stage-dir dist/package --output "dist/${name}.tar.zst"
          ls -la "dist/${name}.tar.zst"
          echo "compressed_size_bytes=$(wc -c < "dist/${name}.tar.zst")"

      - name: Install uv
        uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081 # v5
        with:
          enable-cache: false

      # ── Wheel build (soldr-driven, Linux) ──
      - name: Build wheel through setup-soldr target environment
        if: ${{ !contains(matrix.target, 'unknown-linux-musl') }}
        shell: bash
        env:
          EXPECTED_VERSION: ${{ needs.prepare.outputs.version }}
          SOLDR_RELEASE_CI: "1"
          CC_aarch64_unknown_linux_musl: ${{ matrix.target == 'aarch64-unknown-linux-musl' && 'musl-gcc' || '' }}
          CXX_aarch64_unknown_linux_musl: ${{ matrix.target == 'aarch64-unknown-linux-musl' && 'musl-gcc' || '' }}
          AR_aarch64_unknown_linux_musl: ${{ matrix.target == 'aarch64-unknown-linux-musl' && 'ar' || '' }}
          RANLIB_aarch64_unknown_linux_musl: ${{ matrix.target == 'aarch64-unknown-linux-musl' && 'ranlib' || '' }}
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER: ${{ matrix.target == 'aarch64-unknown-linux-musl' && 'musl-gcc' || '' }}
        run: |
          set -euo pipefail
          rm -f dist/*.whl
          rm -rf target/wheels target/maturin
          find target -name "*.whl" -delete 2>/dev/null || true
          case "$RUNNER_OS" in
            Windows) driver="target/release/soldr.exe" ;;
            *) driver="target/release/soldr" ;;
          esac
          "$driver" cargo clean -p soldr-cli --target "${{ matrix.target }}" --release 2>/dev/null || true
          mkdir -p dist

          expected="${EXPECTED_VERSION#v}"
          git restore -- Cargo.toml Cargo.lock crates/soldr-cli/Cargo.toml || true

          cargo_version="$("$driver" cargo metadata --no-deps --format-version 1 --manifest-path crates/soldr-cli/Cargo.toml 2>/dev/null \
              | python3 -c 'import json,sys; d=json.load(sys.stdin); pkgs=[p for p in d["packages"] if p["name"]=="soldr-cli"]; print(pkgs[0]["version"] if pkgs else "")' 2>/dev/null || true)"
          if [ -z "$cargo_version" ]; then
              echo "ERROR: cargo metadata returned no version for soldr-cli" >&2
              exit 1
          fi
          if [ "$cargo_version" != "$expected" ]; then
              echo "ERROR: soldr-cli version ($cargo_version) does not match prepare.outputs.version ($expected)" >&2
              exit 1
          fi

          wheel_hook='${{ steps.setup_soldr.outputs.target-wheel-hook }}'
          if [ -z "$wheel_hook" ]; then
            wheel_hook='python -m build --wheel'
          fi
          echo "setup-soldr wheel hook: $wheel_hook"
          uv python install 3.13
          uv run --no-project --python 3.13 --with build python \
            .github/scripts/build_release_wheel.py \
            --target "${{ matrix.target }}" \
            --hook "$wheel_hook"

      - name: Build musl wheel in an explicit uv venv
        if: contains(matrix.target, 'unknown-linux-musl')
        shell: bash
        env:
          EXPECTED_VERSION: ${{ needs.prepare.outputs.version }}
          SOLDR_RELEASE_CI: "1"
          CARGO_PROFILE_RELEASE_DEBUG: "0"
          CARGO_PROFILE_RELEASE_LTO: thin
          CARGO_PROFILE_RELEASE_CODEGEN_UNITS: "1"
          CARGO_BUILD_JOBS: "2"
          CC_aarch64_unknown_linux_musl: musl-gcc
          CXX_aarch64_unknown_linux_musl: musl-gcc
          AR_aarch64_unknown_linux_musl: ar
          RANLIB_aarch64_unknown_linux_musl: ranlib
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER: musl-gcc
        run: |
          set -euo pipefail
          python3 .github/scripts/native_release_build.py musl-wheel \
            --driver target/release/soldr \
            --target "${{ matrix.target }}" \
            --expected-version "${EXPECTED_VERSION#v}"

      # soldr#2469 2.2: logic lives in the unit-tested script.
      - name: Lint wheel filenames against workspace version
        run: >-
          python3 .github/scripts/wheel_filename_lints.py version
          --expected-version "${{ needs.prepare.outputs.version }}"

      - name: Assert linux-gnu wheels are tagged manylinux_2_17
        if: contains(matrix.target, 'unknown-linux-gnu')
        # soldr#2469 2.2: logic lives in the unit-tested script.
        run: python3 .github/scripts/wheel_filename_lints.py manylinux

      - name: Assert linux-gnu wheel binary honours the 2.17 floor (soldr#1060)
        if: contains(matrix.target, 'unknown-linux-gnu')
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/verify_wheel_glibc.py --max-glibc 2.17 dist/*.whl

      - name: Assert linux-musl wheels are tagged musllinux_1_2
        if: contains(matrix.target, 'unknown-linux-musl')
        shell: python
        run: |
          import sys
          from pathlib import Path

          wheels = sorted(Path("dist").glob("*.whl"))
          if not wheels:
              sys.exit("no wheels in dist/ - maturin produced nothing?")
          bad = [w.name for w in wheels if "musllinux_1_2" not in w.name]
          if bad:
              msg = ["musllinux_1_2 tag assertion FAILED:"]
              for name in bad:
                  msg.append(f"  - {name}")
              msg.append("")
              msg.append(
                  "Every linux-musl wheel must carry the musllinux_1_2 "
                  "platform tag (PEP 656). A native linux tag or missing "
                  "musllinux tag makes `pip install soldr` on Alpine skip "
                  "the binary wheel and fall back to source build, which is "
                  "the soldr#909 regression."
              )
              sys.exit("\n".join(msg))
          print(f"musllinux_1_2 tag assertion OK: {len(wheels)} wheel(s):")
          for w in wheels:
              print(f"  - {w.name}")

      # soldr#2469 2.2: logic lives in the unit-tested script.
      - name: Verify wheel ships the bin script at the spec path
        run: >-
          python3 .github/scripts/wheel_bin_script_verify.py
          --binary "${{ matrix.binary }}"

      - name: Smoke test wheel
        # GNU ARM64 and Windows are cross-built on Linux. Musl wheels run
        # under Alpine; Windows wheels run in the smoke_windows job below.
        if: |
          !contains(matrix.target, 'musl') &&
          !contains(matrix.target, 'pc-windows-msvc') &&
          matrix.target != 'aarch64-unknown-linux-gnu'
        shell: bash
        env:
          EXPECTED_VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          uv venv .venv
          uv pip install --python .venv dist/*.whl
          if [ -x .venv/bin/soldr ]; then
              soldr_bin=".venv/bin/soldr"
          else
              soldr_bin=".venv/Scripts/soldr.exe"
          fi
          out="$("$soldr_bin" --version)"
          printf 'wheel smoke test — soldr --version output: %s\n' "$out"
          case "$out" in
              "soldr "*) ;;
              *) echo "ERROR: 'soldr --version' output did not start with 'soldr ' — likely shipping a stub binary (soldr#1140)." >&2; exit 1 ;;
          esac

          expected_soldr_version="${EXPECTED_VERSION#v}"
          json_out="$("$soldr_bin" version --json)"
          if [ -z "$json_out" ]; then
              echo "ERROR: 'soldr version --json' produced empty stdout in wheel (soldr#1202)." >&2
              exit 1
          fi
          printf 'wheel smoke test — soldr version --json output: %s\n' "$json_out"
          compact="$(printf '%s' "$json_out" | tr -d ' \n\r\t')"
          if ! printf '%s' "$compact" | grep -q "\"soldr_version\":\"${expected_soldr_version}\""; then
              echo "ERROR: wheel's 'soldr version --json' output does not include soldr_version=${expected_soldr_version} (soldr#1202)." >&2
              exit 1
          fi

      - name: Smoke test musllinux wheel on Alpine
        # Both musl lanes run on matching native-architecture Linux hosts.
        if: contains(matrix.target, 'musl')
        shell: bash
        env:
          EXPECTED_VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          expected_soldr_version="${EXPECTED_VERSION#v}"
          docker run --rm \
            -e EXPECTED_VERSION="$expected_soldr_version" \
            -v "$PWD/dist:/dist:ro" \
            alpine:3.20 \
            sh -euxc '
              apk add --no-cache python3 py3-pip
              python3 -m venv /tmp/venv
              . /tmp/venv/bin/activate
              pip install --no-index --only-binary=:all: --find-links /dist "soldr==${EXPECTED_VERSION}"
              out="$(soldr --version)"
              printf "alpine wheel smoke test - soldr --version output: %s\n" "$out"
              case "$out" in
                "soldr "*) ;;
                *) echo "ERROR: soldr --version output did not start with soldr " >&2; exit 1 ;;
              esac
              json_out="$(soldr version --json)"
              test -n "$json_out"
              printf "alpine wheel smoke test - soldr version --json output: %s\n" "$json_out"
              compact="$(printf "%s" "$json_out" | tr -d " \n\r\t")"
              case "$compact" in
                *"\"soldr_version\":\"${EXPECTED_VERSION}\""*) ;;
                *) echo "ERROR: soldr version --json did not include soldr_version=${EXPECTED_VERSION}" >&2; exit 1 ;;
              esac
            '

      - name: Smoke test standalone musl binary
        # The Alpine wheel smoke above verifies pip compatibility for
        # musllinux. Keep this direct binary smoke too: the standalone
        # musl binary is statically linked so it CAN run on glibc, and
        # it catches broken tarball artifacts before release upload.
        if: contains(matrix.target, 'musl')
        shell: bash
        env:
          EXPECTED_VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          binary="target/${{ matrix.target }}/release/${{ matrix.binary }}"
          test -x "$binary" || { echo "missing release binary: $binary" >&2; exit 1; }
          file "$binary" || true
          "./$binary" --version

          # soldr#1202: guard the `version --json` dispatch-arm code
          # path — v0.7.87 shipped a stub whose `--version` passed but
          # whose `version --json` returned empty stdout.
          expected_soldr_version="${EXPECTED_VERSION#v}"
          json_out="$("./$binary" version --json)"
          if [ -z "$json_out" ]; then
              echo "ERROR: 'soldr version --json' produced empty stdout on musl binary (soldr#1202)." >&2
              exit 1
          fi
          printf 'musl smoke test — soldr version --json output: %s\n' "$json_out"
          compact="$(printf '%s' "$json_out" | tr -d ' \n\r\t')"
          if ! printf '%s' "$compact" | grep -q "\"soldr_version\":\"${expected_soldr_version}\""; then
              echo "ERROR: musl 'soldr version --json' output does not include soldr_version=${expected_soldr_version} (soldr#1202)." >&2
              exit 1
          fi

      - name: Smoke test combined tar.zst archive
        # Round-trip: extract the freshly-packaged combined archive into a
        # scratch dir, list its contents, and run soldr from there. Catches
        # mistakes in the tar layout / file modes / zstd compression /
        # bundled binary names before the artifact reaches the GH
        # release. Cross-arch archives can't be executed (e.g. aarch64
        # build on x86_64 host) so the `--version` step is gated to
        # native-arch combinations.
        shell: bash
        run: |
          set -euo pipefail
          version="${{ needs.prepare.outputs.version }}"
          archive="dist/soldr-${version}-${{ matrix.target }}.tar.zst"
          test -f "$archive" || { echo "missing archive: $archive" >&2; exit 1; }

          extract=$(mktemp -d)
          case "$RUNNER_OS" in
            Windows) driver_suffix=".exe" ;;
            *) driver_suffix="" ;;
          esac
          driver="target/release/soldr${driver_suffix}"
          "$driver" archive --input "$archive" --extract-dir "$extract"
          echo "--- extracted layout ---"
          ls -la "$extract"

          # Every archive must ship soldr, its daemon alias, crgx,
          # cargo-chef, and a manifest.json describing the bundle.
          # Windows archives must also ship soldr's PDB sidecar.
          suffix=""
          case "${{ matrix.target }}" in *-pc-windows-msvc) suffix=".exe" ;; esac
          for required in \
            "${{ matrix.binary }}" \
            "soldr-daemon${suffix}" \
            "crgx${suffix}" \
            "cargo-chef${suffix}" \
            "manifest.json"; do
            test -f "$extract/$required" \
              || { echo "missing $required in $archive" >&2; exit 1; }
          done
          if [ -n "$suffix" ]; then
            if ! find "$extract" -maxdepth 1 -type f \( -name 'soldr.pdb' -o -name 'soldr_cli.pdb' \) | grep -q .; then
              echo "missing soldr PDB sidecar in $archive" >&2
              exit 1
            fi
          fi
          echo "--- extracted manifest.json ---"
          cat "$extract/manifest.json"

          # soldr#1202 follow-up to #1206: cross-arch size-floor guard.
          # The `soldr --version` + `soldr version --json` guards below
          # only run on native-arch matches (aarch64 archives can't
          # execute on x86_64 release runners). A stub binary shipped
          # into a cross-arch archive would slip past every dynamic
          # check. Enforce a 2 MiB floor here so a shipped `soldr` bin
          # that got left as a 332 KiB stub is rejected before it
          # reaches the GH release surface, on every lane. Real soldr
          # ~13-15 MB on Linux glibc / ~15 MB on Windows / ~15 MB on
          # darwin; 2 MiB is well below that but far above any
          # conceivable stub.
          MIN_SOLDR_BYTES=$((2 * 1024 * 1024))
          soldr_size=$(wc -c < "$extract/${{ matrix.binary }}" | tr -d ' ')
          if [ "$soldr_size" -lt "$MIN_SOLDR_BYTES" ]; then
              echo "ERROR: '$extract/${{ matrix.binary }}' is ${soldr_size} bytes; expected >= ${MIN_SOLDR_BYTES} (2 MiB)." >&2
              echo "This is the soldr#1140 / soldr#1202 stub-binary regression — the release archive shipped a placeholder instead of the real soldr binary." >&2
              exit 1
          fi
          echo "archive smoke test — ${{ matrix.binary }} size ${soldr_size} bytes >= ${MIN_SOLDR_BYTES} floor OK"

          # Run soldr only when the archive's target matches the runner's
          # native arch. ubuntu-24.04 / windows-2025 are x86_64; the
          # ubuntu-24.04-arm / windows-11-arm runners are aarch64; macOS is
          # whatever the matrix entry says (Intel vs ARM).
          runner_arch=$(uname -m)
          target="${{ matrix.target }}"
          os_matches=false
          arch_matches=false
          case "$RUNNER_OS:$target" in
            Linux:*-unknown-linux-*|macOS:*-apple-darwin|Windows:*-pc-windows-msvc)
              os_matches=true
              ;;
          esac
          case "$runner_arch:$target" in
            x86_64:x86_64-*|aarch64:aarch64-*|arm64:aarch64-*)
              arch_matches=true
              ;;
          esac
          if [[ "$os_matches" == true && "$arch_matches" == true ]]; then
              "$extract/${{ matrix.binary }}" --version

              # soldr#1202: `soldr version --json` is a distinct code
              # path (Commands::Version arm) from `soldr --version`
              # (clap's built-in --version flag → early exit). v0.7.87
              # shipped a stub whose `soldr --version` printed
              # `soldr 0.0.1` — trivially passing the "starts with
              # soldr " wheel check — while `soldr version --json`
              # produced empty stdout, breaking every downstream JSON
              # consumer including setup-soldr's verify step. Guard
              # both paths here so the class of regression can't
              # recur.
              expected_soldr_version="${version#v}"
              json_out="$("$extract/${{ matrix.binary }}" version --json)"
              if [ -z "$json_out" ]; then
                  echo "ERROR: 'soldr version --json' produced empty stdout — likely a stub binary (soldr#1140 / soldr#1202)." >&2
                  exit 1
              fi
              printf 'soldr version --json output: %s\n' "$json_out"
              # Compact JSON before matching so the pattern survives
              # any whitespace / newline formatting.
              compact="$(printf '%s' "$json_out" | tr -d ' \n\r\t')"
              if ! printf '%s' "$compact" | grep -q "\"soldr_version\":\"${expected_soldr_version}\""; then
                  echo "ERROR: 'soldr version --json' output does not include soldr_version=${expected_soldr_version} (soldr#1202)." >&2
                  exit 1
              fi

              # Smoke the bundled tools too — same arch gate.
              "$extract/soldr-daemon${suffix}" --help >/dev/null
              "$extract/crgx${suffix}" --version
              "$extract/cargo-chef${suffix}" --version
          else
              echo "skipping --version (runner $RUNNER_OS/$runner_arch vs target $target)"
          fi

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: release-soldr-${{ matrix.target }}
          path: dist/soldr-*.tar.zst

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: pypi-soldr-${{ matrix.target }}
          path: dist/*.whl

  # soldr#2453: ARM macOS artifacts are built natively on macos-15; this
  # smoke job downloads and re-verifies the uploaded build-job artifacts
  # on a fresh runner rather than trusting the build lane's own smoke step.
  smoke_macos_arm64:
    name: Smoke ARM macOS release artifacts
    needs:
      - prepare
      - build
    if: needs.prepare.outputs.should_release == 'true'
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      # Checkout only for ci/smoke_release_artifacts.py — the smoke
      # logic lives in a testable Python file, not inline YAML.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ needs.prepare.outputs.commit_sha }}

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: release-soldr-aarch64-apple-darwin
          path: dist

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: pypi-soldr-aarch64-apple-darwin
          path: dist

      - name: Architecture-check and smoke archive and wheel
        shell: bash
        run: |
          set -euo pipefail
          python3 ci/smoke_release_artifacts.py \
            --target aarch64-apple-darwin \
            --expected-version '${{ needs.prepare.outputs.version }}'

  # Windows artifacts are cross-built on Linux; execution still needs native
  # Windows on both architectures. Each smoke consumes uploaded artifacts.
  smoke_windows:
    name: Smoke Windows release artifacts (${{ matrix.arch }})
    needs:
      - prepare
      - build
    if: needs.prepare.outputs.should_release == 'true'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: windows-2025
            target: x86_64-pc-windows-msvc
          - arch: arm64
            runner: windows-11-arm
            target: aarch64-pc-windows-msvc

    steps:
      # Checkout only for ci/smoke_release_artifacts.py — the smoke
      # logic lives in a testable Python file, not inline YAML.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ needs.prepare.outputs.commit_sha }}

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: release-soldr-${{ matrix.target }}
          path: dist

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: pypi-soldr-${{ matrix.target }}
          path: dist

      - name: Setup Python 3.13
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"

      - name: Smoke archive and wheel (incl. required PDB sidecar)
        shell: bash
        run: |
          set -euo pipefail
          python ci/smoke_release_artifacts.py \
            --target '${{ matrix.target }}' \
            --expected-version '${{ needs.prepare.outputs.version }}'

  publish:
    name: Attest and publish release assets
    needs:
      - prepare
      - build
      - smoke_macos_arm64
      - smoke_windows
    if: needs.prepare.outputs.should_publish_github_release == 'true' && needs.smoke_macos_arm64.result == 'success' && needs.smoke_windows.result == 'success'
    runs-on: ubuntu-24.04
    # Artifact download + attestation; observed max 0.6 min.
    timeout-minutes: 30

    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: dist
          pattern: release-soldr-*
          merge-multiple: true

      # Wheels ship as GH-release assets alongside the .tar.zst archives.
      # This is parity with what `publish-pypi` uploads to PyPI: the same
      # 8 platform wheels become offline-installable via
      # `pip install <release-asset-url>`, and the assets are attestation-
      # covered via the same SHA256SUMS file below.
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: dist
          pattern: pypi-soldr-*
          merge-multiple: true

      - name: Generate release checksums
        shell: bash
        run: |
          set -euo pipefail
          cd dist
          # Glob expansion happens before the `>` redirect creates the
          # output file, so SHA256SUMS.txt is never present in the set
          # it's writing.
          sha256sum soldr-* > "soldr-${{ needs.prepare.outputs.version }}-SHA256SUMS.txt"

      - uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3
        with:
          subject-checksums: dist/soldr-${{ needs.prepare.outputs.version }}-SHA256SUMS.txt

      # Pre-create the tag via the git refs API instead of letting
      # `gh release create --target <sha>` create it implicitly.
      # Rationale: on workflow_dispatch runs (vs push) the GITHUB_TOKEN
      # cannot create-via-release at a protected branch's HEAD — `gh
      # release create` returns "HTTP 403: Resource not accessible by
      # integration" even though the job log shows `Contents: write`.
      # The /git/refs endpoint accepts the same token at this repo, so
      # creating the tag first lets the subsequent release-create see an
      # existing tag and skip the implicit-tag-creation that was the
      # source of the 403.
      #
      # soldr#1252: both endpoints started returning 403 for v0.7.95 even
      # on push runs. Owner may need to pre-populate the tag out-of-band
      # with a PAT. Make the step idempotent: check if the tag already
      # exists first (owner may have pre-populated it), skip if so, else
      # attempt the create. The check-first flow also makes reruns after
      # a partial success safe.
      - name: Pre-create release tag
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          tag='${{ needs.prepare.outputs.version }}'
          sha='${{ needs.prepare.outputs.commit_sha }}'
          if gh api "repos/${GITHUB_REPOSITORY}/git/refs/tags/${tag}" >/dev/null 2>&1; then
              echo "tag ${tag} already exists — skipping create"
              exit 0
          fi
          if ! gh api "repos/${GITHUB_REPOSITORY}/git/refs" \
              -X POST \
              -f "ref=refs/tags/${tag}" \
              -f "sha=${sha}"; then
              echo "::error::GITHUB_TOKEN could not create tag ${tag} via /git/refs. Registry publishes are blocked until the GitHub release tag/assets can be created. See soldr#1252."
              {
                echo "## Manual release recovery needed (tag)"
                echo '```'
                echo "gh api repos/${GITHUB_REPOSITORY}/git/refs -X POST -f ref=refs/tags/${tag} -f sha=${sha}"
                echo '```'
              } >> "$GITHUB_STEP_SUMMARY"
              exit 1
          fi

      - name: Create draft GitHub Release
        id: create_release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          tag='${{ needs.prepare.outputs.version }}'
          # Idempotent: reruns after a partial success (or an owner-side
          # manual create) must not fail on "release already exists".
          if gh release view "${tag}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
              echo "release ${tag} already exists; uploading/replacing assets"
              gh release upload "${tag}" dist/* --repo "${GITHUB_REPOSITORY}" --clobber
              echo "created=true" >> "$GITHUB_OUTPUT"
              exit 0
          fi
          if gh release create "${tag}" dist/* \
            --repo "${GITHUB_REPOSITORY}" \
            --draft \
            --generate-notes \
            --target "${{ needs.prepare.outputs.commit_sha }}" \
            --title "${tag}"; then
              echo "created=true" >> "$GITHUB_OUTPUT"
          else
              echo "created=false" >> "$GITHUB_OUTPUT"
              echo "::error::GITHUB_TOKEN could not create the GitHub release for ${tag}. Registry publishes are blocked until the GitHub release exists with the expected assets. See soldr#1252."
              {
                echo "## Manual release recovery needed (GitHub release)"
                echo '```'
                echo "gh run download ${GITHUB_RUN_ID} --repo ${GITHUB_REPOSITORY} --pattern 'release-soldr-*' --dir dist"
                echo "gh release create ${tag} dist/* --repo ${GITHUB_REPOSITORY} --generate-notes --target ${{ needs.prepare.outputs.commit_sha }} --title ${tag}"
                echo '```'
              } >> "$GITHUB_STEP_SUMMARY"
              exit 1
          fi

      - name: Publish GitHub Release
        if: steps.create_release.outputs.created == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh release edit "${{ needs.prepare.outputs.version }}" \
            --repo "${GITHUB_REPOSITORY}" \
            --draft=false

  verify_github_release:
    name: Verify GitHub Release assets
    needs:
      - prepare
      - build
      - publish
    if: always() && needs.prepare.outputs.should_release == 'true' && needs.build.result == 'success' && needs.prepare.outputs.github_release_immutable != 'true'
    runs-on: ubuntu-24.04
    # Asset-set assertion against the GitHub API; observed max 0.1 min.
    timeout-minutes: 15

    steps:
      # Needed since soldr#2469 step 2.2: the expected-asset list is
      # generated from ci/canonical-targets.json by a repo script.
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Verify expected release assets
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          tag='${{ needs.prepare.outputs.version }}'
          cargo_version="${tag#v}"
          release_json="$(gh release view "$tag" --repo "$GITHUB_REPOSITORY" --json assets,isDraft)"
          if [[ "$(jq -r '.isDraft' <<< "$release_json")" != "false" ]]; then
            echo "GitHub release $tag is still a draft" >&2
            exit 1
          fi
          # Contract-generated (soldr#2469 step 2.2) — see the prepare job.
          expected_asset_list="$(python3 .github/scripts/release_completeness.py --version "$tag" --list-expected-github-assets)"
          expected_assets=()
          while IFS= read -r asset; do
            expected_assets+=("$asset")
          done <<< "$expected_asset_list"
          missing=()
          for asset in "${expected_assets[@]}"; do
            size="$(jq --arg name "$asset" -r '.assets[] | select(.name == $name) | .size' <<< "$release_json" | head -n1)"
            if [[ -z "$size" || "$size" == "null" ]]; then
              missing+=("$asset")
              continue
            fi
            if (( size <= 0 )); then
              echo "GitHub release asset $asset has invalid size $size" >&2
              exit 1
            fi
          done
          if (( ${#missing[@]} > 0 )); then
            printf 'GitHub release %s is missing expected assets:\n' "$tag" >&2
            printf ' - %s\n' "${missing[@]}" >&2
            exit 1
          fi
          echo "GitHub release $tag has all ${#expected_assets[@]} expected assets."

  publish-pypi:
    name: Publish hardened PyPI wheels
    needs:
      - prepare
      - build
      - verify_github_release
    # Explicit recovery can add missing PyPI files when an earlier partial
    # publish made the corresponding GitHub Release immutable.
    if: always() && needs.prepare.outputs.should_publish_pypi == 'true' && (needs.verify_github_release.result == 'success' || (needs.prepare.outputs.github_release_immutable == 'true' && github.event_name == 'workflow_dispatch' && inputs.force_pypi_publish))
    runs-on: ubuntu-24.04
    # Wheel download + twine upload; observed max 1.9 min.
    timeout-minutes: 30

    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: dist
          pattern: pypi-soldr-*
          merge-multiple: true

      - name: Show Python distributions
        shell: bash
        run: |
          set -euo pipefail
          ls -1 dist

      - name: Publish wheel set to PyPI
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b # release/v1
        with:
          packages-dir: dist
          skip-existing: true

      - name: Verify all wheels visible on PyPI
        # Poll until all sequential uploads are visible so downstream
        # resolvers cannot cache a transient "no compatible wheel" result.
        shell: bash
        env:
          PYPI_VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          set -euo pipefail
          # Tag-style version is `vX.Y.Z`; PyPI keys releases by `X.Y.Z`.
          pypi_version="${PYPI_VERSION#v}"
          # Expected wheel count == number of PyPI wheel matrix rows in the
          # `build` job: two architectures for glibc, musl, macOS, and Windows.
          expected=8
          deadline=$(( $(date +%s) + 1800 ))
          attempts=0
          max_attempts=60
          while :; do
            attempts=$((attempts + 1))
            count="$(curl -fsSL --max-time 30 --connect-timeout 10 "https://pypi.org/pypi/soldr/${pypi_version}/json" \
              | python3 -c 'import json,sys; print(len(json.load(sys.stdin).get("urls",[])))' 2>/dev/null \
              || echo 0)"
            echo "PyPI reports ${count}/${expected} files for soldr==${pypi_version}"
            if [ "$count" -ge "$expected" ]; then
              echo "All ${expected} wheels visible on PyPI; verification complete."
              exit 0
            fi
            if [ "$attempts" -ge "$max_attempts" ]; then
              echo "Timed out after ${max_attempts} attempts waiting for ${expected} wheels for soldr==${pypi_version}" >&2
              exit 1
            fi
            if [ "$(date +%s)" -ge "$deadline" ]; then
              echo "Timed out waiting for ${expected} wheels for soldr==${pypi_version}" >&2
              exit 1
            fi
            sleep 15
          done

  publish-npm:
    name: Publish npm package
    needs:
      - prepare
      - build
      - verify_github_release
      - publish-pypi
    if: always() && ((github.event_name == 'workflow_dispatch' && inputs.npm_release_ref != '') || (needs.prepare.outputs.should_publish_npm == 'true' && needs.verify_github_release.result == 'success' && (needs.publish-pypi.result == 'success' || needs.publish-pypi.result == 'skipped')))
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment: release
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        working-directory: release-source
    steps:
      - name: Checkout release controls
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          path: release-control
      - name: Checkout npm package source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          submodules: recursive
          ref: ${{ inputs.npm_release_ref != '' && format('refs/tags/{0}', inputs.npm_release_ref) || needs.prepare.outputs.commit_sha }}
          path: release-source
      - name: Validate immutable npm recovery release
        if: github.event_name == 'workflow_dispatch' && inputs.npm_release_ref != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          python3 ../release-control/.github/scripts/validate_npm_release_recovery.py
          --repository "$GITHUB_REPOSITORY"
          --release-ref "${{ inputs.npm_release_ref }}"
          --source-dir .
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org
      - name: Install npm CLI with trusted publishing support
        shell: bash
        run: npm install --global npm@11.12.1
      - name: Validate npm package
        run: node scripts/test-npm-package.js && npm pack --dry-run
      - name: Publish package to npm
        run: python3 ../release-control/.github/scripts/publish_npm_package.py --source-dir .

  # soldr#2469 step 1.1: terminal truth check. Green must mean the whole
  # public surface (17 GitHub assets from the canonical-targets contract,
  # the PyPI wheel set, the npm version) is verified complete for this
  # release ref. always() so upstream skips (the 0.9.0 false-green path:
  # verify_github_release skipped on an already-immutable release) cannot
  # exempt the run from the check. Recovery-only npm dispatches carry no
  # should_release and are reported as recovery by their own job, never
  # as a normal release result.
  release-completeness:
    name: Release surface completeness gate
    needs:
      - prepare
      - publish
      - verify_github_release
      - publish-pypi
      - publish-npm
    if: always() && needs.prepare.outputs.should_release == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Verify every public release surface is complete
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          python3 .github/scripts/release_completeness.py
          --version "${{ needs.prepare.outputs.version }}"
