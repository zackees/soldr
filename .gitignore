# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Generated commands
/zcmds/bin/

# C extensions
*.so

# Distribution / packaging
.Python
prod_env/
data/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Repo-local CARGO_HOME cache (perf_local.py / Docker harnesses point
# CARGO_HOME at ./.cargo). The tracked `.cargo/config.toml` stays; only the
# downloaded/build caches are ignored so `git add -A` can never sweep them in.
/.cargo/registry/
/.cargo/git/
/.cargo/.global-cache
/.cargo/.package-cache
/.cargo/.package-cache-mutate

# Same caches, one level down. Every dylint crate carries its own
# `.cargo/config.toml` (it selects the `dylint-link` linker), and running its
# test suite fills that directory with a registry cache -- 141 MB for a single
# `cargo test` run, none of it ignored by the root-anchored rules above. The
# tracked `config.toml` stays; only the caches are ignored (soldr#2749).
dylints/*/.cargo/registry/
dylints/*/.cargo/git/
dylints/*/.cargo/.global-cache
dylints/*/.cargo/.package-cache
dylints/*/.cargo/.package-cache-mutate

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
*rclone.conf
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/
.activate.sh
activate

# Generated by mac
**/.DS_Store
uv.lock

# Claude local workspace/runtime files. Keep .claude/ itself trackable for shared
# skills, hooks, and other repo configuration.
.claude/scheduled_tasks.lock
.claude/workspaces/
.claude/worktrees/
.claude/settings.local.json
.claude/.credentials*

# clud project config/runtime state: track shared settings while keeping
# generated Docker harnesses, task ledgers, loops, and local overrides private.
.clud/*
!.clud/
!.clud/settings.json
.clud/settings.local.json

# Dependent-repo checkouts coordinated via the clud-extern-repos convention.
.extern-repos/

# `ci/perf_local.py` copies debug traces out of the Linux runner into
# `.perf-local/` inside the repository mount (see `--debug-trace`), and agents
# park scratch logs beside them. It is per-developer runtime output, never
# repository content.
.perf-local/

# nektos/act local workflow runs (soldr#3039): the local cache/artifact
# servers and the tee'd run logs are per-developer runtime output. `.actrc`
# itself IS repository content and stays tracked.
.act-cache/
.act-artifacts/
act-run-*.log

# `soldr dylint cook` takes an advisory workspace-source lock so two nightlies
# cannot be inside cargo-chef's in-place skeleton reconstruction at once. In a
# git checkout it lives under `.git/`, but each crate under `dylints/` is its
# own Cargo workspace with no `.git/` of its own, so the lock lands beside the
# manifest instead. It holds no state (soldr#3042).
.soldr-dylint-cook.lock
