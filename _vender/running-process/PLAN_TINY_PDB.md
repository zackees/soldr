# Tiny PDB Plan

The active tiny-PDB follow-up task now lives in [docs/AGENT_TASKS.md](C:/Users/niteris/dev/running-process/docs/AGENT_TASKS.md).

This root planning file is kept only as a breadcrumb so older references do not point at a deleted path.
